from django.db import models
#untuk membuat model atau struktur data yang akan digunakan dalam aplikasi kita. Model adalah sebuah kelas yang digunakan untuk merepresentasikan tabel dalam database. 
# Setiap atribut dalam kelas model akan menjadi kolom dalam tabel database, dan setiap instance dari kelas model akan menjadi baris dalam tabel database.
# Create your models here.
#struktur database yang akan digunakan dalam aplikasi kita, seperti nama tabel, kolom, tipe data, dan lain-lain. Model biasanya didefinisikan dalam file models.py di dalam direktori aplikasi kita.
